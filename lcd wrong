#include<reg51.h>
void delay(unsigned int time)
{
int i,j;
for(i=0;,time;i++)
for(j=0;j<3000;j++);
}
void main()
{
Relay 
Device pins 8051 pins
Relay P2.4
P2=0;
while(1)
{
P2|=1<<4;
delay(300);
P2=0;
delay(300);
}
}
